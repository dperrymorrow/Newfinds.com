section.promoted
  nav
    ul
      for article in public.posts._data
        li
          a(href="/posts/#{ article.slug }" class=current.source === article.slug ? 'active' : '')
            = article.title
